name: filtros_vision_app
description: Aplicacion movil de filtros de vision artificial con OpenCV
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  image_picker: ^1.0.4
  permission_handler: ^11.0.1
  http: ^1.1.0
  path_provider: ^2.1.1
  image_gallery_saver_plus: ^4.0.1
  flutter_spinkit: ^5.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
